{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Api-Resources Server",
      "type": "php",
      "request": "launch",
      "port": 9000,
      "spathMappings": {
        "/api-resources": "${workspaceFolder}/../..",
      }
    }
  ]
}
